const siteName = "/cms/";
export const Urls = {
    pagesController : siteName + "api/pages/",
    catController : siteName + "api/cats/",
    base : siteName
}
const base = siteName;
export const NavUrls ={
   
    home : base ,
}